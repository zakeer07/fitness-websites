/**
 * Copy to auth-config.js and fill in your Supabase project (optional).
 * Without this file, accounts use secure local storage on this device only.
 *
 * Supabase: Project Settings → API → Project URL + anon public key
 * Run supabase/schema.sql in the SQL editor after creating the project.
 */
window.FITNESS_AUTH_CONFIG = {
  supabaseUrl: "https://YOUR_PROJECT.supabase.co",
  supabaseAnonKey: "YOUR_ANON_KEY",
};
